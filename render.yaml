services:
  - type: web
    name: gis-full-stack
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATA_MODE
        value: demo
      - key: USE_DEMO_DATA
        value: "true"
      - key: DATABASE_URL
        sync: false
